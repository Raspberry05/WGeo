# Copy to .env.local for local development

# OpenSky OAuth (server-only — set in Vercel Environment Variables)
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=

# Cesium Ion (browser — terrain/imagery)
NEXT_PUBLIC_CESIUM_ION_TOKEN=
